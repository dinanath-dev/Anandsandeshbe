{
  "name": "ssdn-web",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend",
    "dev": "npm run dev --prefix backend",
    "dev:full": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "sh -c 'test -f frontend/package.json && npm run build --prefix frontend || echo \"No frontend/package.json — skip build.\"'",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
